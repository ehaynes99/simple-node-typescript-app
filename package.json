{
  "name": "simple-node-typescript-app",
  "version": "0.0.0",
  "author": "Eric Haynes",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "lint-staged": {
    "*.ts": [
      "eslint"
    ]
  },
  "scripts": {
    "build": "rimraf dist && tsc",
    "test": "jest",
    "precommit": "lint-staged",
    "start": "node dist/index.js",
    "start:dev": "ts-node src/index.ts",
    "prepare": "husky install",
    "prepublishOnly": "npm run test && npm run build"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "@types/node": "^15.12.1",
    "eslint-config-erich-node": "^1.1.7",
    "husky": "^6.0.0",
    "jest": "^27.0.4",
    "lint-staged": "^11.0.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.0.3",
    "ts-node": "^10.0.0",
    "typescript": "^4.3.2"
  }
}
