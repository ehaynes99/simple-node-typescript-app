{
  "name": "simple-node-typescript-app",
  "version": "0.0.0",
  "author": "Eric Haynes",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "lint-staged": {
    "*.ts": [
      "eslint"
    ]
  },
  "scripts": {
    "build": "rimraf dist && tsc",
    "test": "jest",
    "precommit": "lint-staged",
    "start": "node dist/index.js",
    "start:dev": "ts-node src/index.ts",
    "prepare": "husky install",
    "prepublishOnly": "npm run test && npm run build"
  },
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "@types/node": "^16.9.2",
    "eslint-config-erich-node": "^1.1.10",
    "husky": "^7.0.2",
    "jest": "^27.2.0",
    "lint-staged": "^11.1.2",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.3"
  }
}
